// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("mysql://admin:BBg3Ytah@165.73.249.103:18937/nextauth?schema=public")
}

model User {
  id        Int     @id @default(autoincrement())
  email     String  @unique
  password  String
}